package views

import (
	"strconv"

	"github.com/Loxstomper/harness/internal/controlroom/query"
)

// ReplayPage is the invocation replay view (specs/observability.md, "Replayability — the
// differentiator"; specs/control-room.md, T4.11): the reconstructed decision trail of one
// invocation — exactly what the LLM saw and did, turn by turn — parsed from the
// broker-captured transcript in the artifact store. It is the forensic answer to "why did
// the agent do that": when an autonomous change looks wrong, this is how a human answers it
// with certainty rather than guessing, and it is the audit mechanism that makes a
// no-human-review system accountable.
//
// Like the issue-detail page it drills from, it is a forensic snapshot, not a feed — plainly
// server-rendered with no SSE refresh. When no trail can be reconstructed (the transcript is
// not reachable, or could not be decoded) it renders a notice rather than failing, offering
// the raw-bytes link when a hash is known.
templ ReplayPage(rep query.Replay) {
	@Layout("Harness — Replay "+rep.Issue.ID, "") {
		<div class="mx-auto max-w-4xl space-y-6">
			<div class="space-y-2">
				<div class="flex flex-wrap items-center gap-3">
					<span class="font-mono text-sm text-slate-500">{ rep.Issue.ID }</span>
					@statusBadge(rep.Issue.Status)
					if rep.Merged {
						<span class="rounded bg-emerald-500/20 px-1.5 py-0.5 text-xs text-emerald-300">merged</span>
					}
				</div>
				<h1 class="text-2xl font-semibold tracking-tight">Replay — { orDash(rep.Issue.Title) }</h1>
				<div class="flex flex-wrap items-center gap-4 text-xs">
					<a href={ templ.URL("/issue/" + rep.Issue.ID) } class="text-sky-400 hover:text-sky-300 hover:underline">← Issue detail</a>
					if rep.Hash != "" {
						<a
							href={ templ.URL("/artifact/" + rep.Hash) }
							target="_blank"
							rel="noopener"
							class="font-mono text-slate-500 hover:text-slate-300 hover:underline"
						>raw transcript { shortHash(rep.Hash) }</a>
					}
				</div>
			</div>
			if !rep.Available {
				@replayNotice(rep)
			} else {
				if rep.System != "" {
					<section class="space-y-2">
						<h2 class="text-sm font-medium text-slate-400">System</h2>
						<pre class="whitespace-pre-wrap break-words rounded-lg border border-slate-800 bg-slate-900/40 p-4 text-xs text-slate-300">{ rep.System }</pre>
					</section>
				}
				<section class="space-y-3">
					<div class="flex items-baseline justify-between">
						<h2 class="text-sm font-medium text-slate-400">Decision trail</h2>
						<span class="text-xs text-slate-500">
							{ strconv.Itoa(len(rep.Turns)) } turns · { strconv.Itoa(rep.TotalInput) } in · { strconv.Itoa(rep.TotalOutput) } out
						</span>
					</div>
					for _, t := range rep.Turns {
						@replayTurn(t)
					}
				</section>
			}
		</div>
	}
}

// replayNotice explains why there is no trail to render. A known hash that could not be
// loaded points the human at the raw bytes; no hash at all means none was retained (the
// trail lives only on the merge trailer — unmerged or unharvested work has none).
templ replayNotice(rep query.Replay) {
	<div class="rounded-lg border border-slate-800 bg-slate-900/40 p-6">
		if rep.Hash != "" {
			<p class="text-sm text-slate-300">The transcript for this invocation could not be loaded from the artifact store.</p>
			<p class="mt-1 text-xs text-slate-500">Its content address is <span class="font-mono">{ rep.Hash }</span> — try the raw-bytes link above.</p>
		} else {
			<p class="text-sm text-slate-300">No decision trail was captured for this invocation.</p>
			<p class="mt-1 text-xs text-slate-500">The transcript is retained only for work merged to main; this issue has not merged, or no transcript was harvested.</p>
		}
	</div>
}

// replayTurn renders one llm-turn: its inbound context (what the model newly saw), the
// model's response, the tools it asked to call, and the turn's stop reason and token cost.
templ replayTurn(t query.ReplayTurn) {
	<article class="rounded-lg border border-slate-800 bg-slate-900/40">
		<header class="flex flex-wrap items-center justify-between gap-2 border-b border-slate-800 px-4 py-2">
			<span class="text-sm font-medium text-slate-300">Turn { strconv.Itoa(t.Index) }</span>
			<div class="flex items-center gap-3 text-xs text-slate-500">
				@replayStopBadge(t.Stop)
				<span class="tabular-nums">{ strconv.Itoa(t.InputTokens) } in · { strconv.Itoa(t.OutputTokens) } out</span>
				if t.CacheRead > 0 {
					<span class="tabular-nums text-slate-600">{ strconv.Itoa(t.CacheRead) } cached</span>
				}
			</div>
		</header>
		<div class="space-y-3 p-4">
			for _, m := range t.Inbound {
				@replayInbound(m)
			}
			if t.Text != "" {
				<div class="space-y-1">
					<div class="text-xs font-medium uppercase tracking-wide text-emerald-400">assistant</div>
					<pre class="whitespace-pre-wrap break-words rounded-md border border-slate-800 bg-slate-950/60 p-3 text-sm text-slate-100">{ t.Text }</pre>
				</div>
			}
			for _, tc := range t.ToolCalls {
				@replayToolCall(tc)
			}
		</div>
	</article>
}

// replayInbound renders one message the model saw at the start of a turn — the opening
// brief or a tool result fed back. A tool result that reported an error is tinted so a
// failure the model had to recover from reads at a glance.
templ replayInbound(m query.ReplayMessage) {
	<div class="space-y-1">
		<div class="text-xs font-medium uppercase tracking-wide text-slate-500">{ orDash(m.Role) }</div>
		if m.Text != "" {
			<pre class="whitespace-pre-wrap break-words rounded-md border border-slate-800 bg-slate-950/40 p-3 text-sm text-slate-300">{ m.Text }</pre>
		}
		for _, tr := range m.ToolResults {
			if tr.IsError {
				<pre class="whitespace-pre-wrap break-words rounded-md border border-rose-900/50 bg-rose-950/20 p-3 text-xs text-rose-200">{ tr.Content }</pre>
			} else {
				<pre class="whitespace-pre-wrap break-words rounded-md border border-slate-800 bg-slate-950/40 p-3 text-xs text-slate-400">{ tr.Content }</pre>
			}
		}
	</div>
}

// replayToolCall renders one tool the model asked to invoke and the arguments it passed.
templ replayToolCall(tc query.ReplayToolCall) {
	<div class="space-y-1">
		<div class="flex items-center gap-2 text-xs">
			<span class="font-medium uppercase tracking-wide text-indigo-400">tool call</span>
			<span class="font-mono text-slate-200">{ tc.Name }</span>
		</div>
		if tc.Args != "" {
			<pre class="overflow-x-auto whitespace-pre rounded-md border border-indigo-900/40 bg-slate-950/60 p-3 text-xs text-indigo-200">{ tc.Args }</pre>
		}
	</div>
}

// replayStopBadge tints the turn's stop reason: tool_use (the loop continues) reads
// distinct from a natural end_turn, and a max_tokens truncation stands out as a budget
// signal. The class literals stay inline (a templ switch, not a Go helper) so the Tailwind
// scanner sees them, like statusBadge.
templ replayStopBadge(stop string) {
	switch stop {
		case "tool_use":
			<span class="rounded-full bg-indigo-500/20 px-2 py-0.5 text-indigo-300">{ stop }</span>
		case "end_turn":
			<span class="rounded-full bg-emerald-500/20 px-2 py-0.5 text-emerald-300">{ stop }</span>
		case "max_tokens":
			<span class="rounded-full bg-amber-500/20 px-2 py-0.5 text-amber-300">{ stop }</span>
		case "":
			<span class="rounded-full bg-slate-700/40 px-2 py-0.5 text-slate-400">—</span>
		default:
			<span class="rounded-full bg-slate-700/40 px-2 py-0.5 text-slate-300">{ stop }</span>
	}
}

// ReplayNotAttached renders the replay chrome with a single notice instead of a trail —
// used when the control room is not attached to a running factory (a standalone `harness
// serve` has no stores to read) or when the issue cannot be loaded (an unknown id). It is the
// page-level fallback (a missing/undecodable transcript for a *known* issue is handled
// inline by ReplayPage's notice, which still shows the issue header).
templ ReplayNotAttached(msg string) {
	@Layout("Harness — Replay", "") {
		<div class="mx-auto max-w-3xl">
			<h1 class="text-xl font-semibold tracking-tight">Replay</h1>
			<p class="mt-2 text-slate-400">{ msg }</p>
		</div>
	}
}
