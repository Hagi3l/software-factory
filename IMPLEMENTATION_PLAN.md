# Implementation Plan

Build order for the harness, derived from [specs/](specs/README.md). The spine is
[bootstrap.md](specs/bootstrap.md): hand-build a minimal kernel that does
`spec → implement → gate → merge` for one issue (the **self-host point**), then let
the harness build its own remaining features as beads issues.

## How to read this

- **Phases 0–1** (scaffolding + kernel) are broken into **atomic tasks** — each is
  one self-contained unit of work with a single concern, completable and verifiable
  on its own. These are designed to map 1:1 onto future beads seed issues (the
  harness's own unit: one issue = one invocation).
- **Phases 2–5** are **coarse** on purpose. They are the post-kernel work the harness
  largely builds for itself; detailing them now risks planning against a kernel that
  doesn't exist yet. Expand each into atomic tasks when its phase begins.
- Tasks within a phase are listed in dependency order. Cross-task deps are noted as
  `(needs Tx.y)` only where they aren't the obvious linear predecessor.
- `(spec)` links point at the authoritative contract for that task.

## The self-host milestone

Phase 1 ends at the kernel from [bootstrap.md](specs/bootstrap.md): config →
beads → sandbox → runner/broker → agent loop → gate runner → orchestrator loop.
Bootstrap simplifications hold until then — DAG collapses to
`implement → gate → integrate`, merge queue is trivial (single stream, no
rebase/re-gate), NATS is in-process, Docker stands in for Firecracker, no control
room (CLI-driven), the implementor writes its own tests.

## TCB caveat

Per [bootstrap.md](specs/bootstrap.md), the components that *enforce* the guarantees —
orchestrator, runner/broker, sandbox, gate harness — are the Trusted Computing Base.
**TCB-touching changes stay human-reviewed even after self-hosting.** Autonomy is
earned first for non-TCB work (new souls, stages, the control room).

---

## Phase 0 — Scaffolding & foundations

> **Environment findings (from T0.1, 2026-05-28):**
> - Go toolchain `go1.26.3` (darwin/arm64); `go.mod` pins `go 1.26`. Module path `github.com/Loxstomper/harness` (from git remote `git@github.com:Loxstomper/harness.git`).
> - Build tooling: `make` is present, `just` is **not** → the `Makefile` is canonical. Targets: `build vet lint fmt tidy test test-unit clean`. `make test-unit` emits `go test -json` to `test/results/` per CLAUDE.md.
> - `bd` (beads CLI) is now **installed** at `/home/clanker/.local/bin/bd`, v0.62.0 (was missing at T0.1 time) — T1.3/T1.4 are unblocked.
> - `golangci-lint` v2.12.2 **installed** (`brew install golangci-lint`); `make check` (vet + lint + test-unit) is the local gate and is clean. The `misspell` linter is `locale: US`, so use US spellings in Go comments (`behavior`/`neighbors`/`fulfill`/`modeled`) — specs may stay British. `golangci-lint config verify` passes. `.golangci.yml` was pruned of the cruft templated from another project (the `internal/controlplane/...` rules, pgx/SQL `errcheck` exclusions, and the `websocket.Dial` rule); the generic `_test\.go` relaxation and the `.*_templ\.go$` exclusion are kept (the latter for the planned templ control room, Phase 4).
> - T0.2 domain types live in a new dependency-free leaf package `internal/core` (`Soul`, `Issue`, `Brief`, `Result`, `ResultStatus`, plus `Branch`/`Evidence`/`ArtifactRef`/`Proposal`) — NOT in `internal/agent`. Rationale: keep pure data types out of the behavioural `agent` package (which will import `model`/`broker`) so `beads`/`orchestrator`/`runner`/`gate` can share them without coupling or import-cycle risk. This adds `core` to the T0.1-enumerated layout. The behavioural `Agent` interface stays deferred to T1.13.
> - Issue lifecycle status (ready/in_progress/blocked/done) is NOT yet an enum — the specs don't enumerate fixed values and `bd` isn't installed. `core.Issue` has no Status field; model it during beads integration (T1.3/T1.4). The only status enum defined so far is `core.ResultStatus` (done/failed/needs-spec-clarification).
> - T0.3 canonical model types live in `internal/model` (`ToolDef`, `ToolCall`, `ToolResult`, `Message`, `Response`, `Usage`, `StopReason`, `Role`, plus a `JSONSchema = json.RawMessage` alias). A canonical `Request` type is intentionally deferred to T1.8 (adapter interface) — the spec only describes it in prose and its optional fields (prompt caching, extended thinking) are shaped by the streaming adapter interface. `ToolResult`/`Role`/`StopReason`/`Usage` field shapes were derived from models.md prose (provider tool-call divergence, usage normalization incl. cache tokens, the finish reasons the loop branches on); they are normalized canonical values that adapters translate to/from provider wire formats.
> - T0.4 messaging lives in a new package `internal/messaging` (NOT `internal/nats`, which would alias-collide with the upstream `nats` client package). Deps added: `github.com/nats-io/nats.go v1.52.0` + `github.com/nats-io/nats-server/v2 v2.14.1` (now direct in go.mod). The embedded server uses `DontListen` + in-process client connect (`nats.InProcessServer`) — true in-process transport, swappable for a TCP listener when distribution lands (Phase 5). Subjects are built via helpers (`WorkSubject`/`ResultSubject`/`AgentEventsSubject`/`ControlSubject`) + constants (`SubjectDLQ`, `WorkStreamSubjects`, `ResultStreamSubjects`, `ControlSubjects`). JetStream streams: `HARNESS_WORK` (WorkQueue retention, ack=lease), `HARNESS_RESULT` (Limits, 7d max-age), `HARNESS_DLQ` (Limits, no max-age). Consumer helpers: `EnsureWorkConsumer(role, ackWait)`, `EnsureResultConsumer`. Events/control are core NATS (no stream). Retention values are bootstrap defaults — the spec marks concrete retention/replicas as OPEN.

- [x] **T0.1 Go module + repo layout** — `go.mod`, `cmd/harness/`, `internal/{config,beads,sandbox,runner,broker,agent,model,gate,orchestrator,artifact}/`, build target (`make`/`just` running `go build`). ([architecture.md](specs/architecture.md))
- [x] **T0.2 Core domain types** — `Soul`, `Brief`, `Result` (status: `done|failed|needs-spec-clarification`), `Issue`, status enum. Pure structs, no behaviour. ([components/agent.md](specs/components/agent.md))
- [x] **T0.3 Canonical model types** — `ToolDef`, `ToolCall`, `ToolResult`, `Message`, `Response`, `Usage`, `StopReason`, `Role`. ([models.md](specs/models.md))
- [x] **T0.4 In-process NATS + subject taxonomy** — embed `nats-server` (still spoken in-process per location transparency); define subject constants (`harness.work.<role>`, `harness.result.<role>`, `harness.agent.<id>.events`, `harness.dlq`, `harness.control.*`) and JetStream stream/consumer setup for work/result/dlq. ([messaging.md](specs/messaging.md))

**Phase 0 complete.** Scaffolding, core domain types, canonical model types, and the in-process NATS layer are in place. `bd` (beads CLI) v0.62.0 is now installed (T1.3/T1.4 unblocked). `golangci-lint` v2.12.2 is installed and `make check` passes.

## Phase 1 — Kernel → self-host point

> **T1.1 findings (2026-05-28):**
> - Config package implemented in `internal/config`: `Harness`/`Stage`/`Policy`/`Budget` (harness.yaml), `Infra` + `SandboxConfig`/`SandboxLimits`/`NATSConfig`/`JetStreamConfig`/`BrokerConfig`/`ArtifactsConfig`/`OTelConfig`/`ModelProvider` (infra.<env>.yaml), and a custom `Duration` type. Loaders: `LoadHarness`/`LoadSouls`/`LoadInfra` plus aggregate `Load(dir, env)` → `*Config{Root, Harness, Souls, Infra}` with `(*Config).PersonaPath(soul)`.
> - Dependency added: `gopkg.in/yaml.v3 v3.0.1` (now direct in go.mod) — the only YAML lib; chosen for direct struct unmarshal. Verified yaml.v3 parses underscore int literals natively (`tokens: 2_000_000` → 2000000), so the spec's syntax loads as written.
> - **Souls unmarshal directly into `core.Soul`** (no parallel `config.Soul` — single source of truth). Added the missing `Selector map[string]string` field + yaml tags to `core.Soul`; soul.go's doc promised "loading a soul YAML populates this struct directly" but the struct had omitted `selector`. This completes the T0.2 soul type.
> - **Strict parsing**: loaders use `yaml.Decoder.KnownFields(true)` — an unknown/typo'd key is a loud load error, per configuration.md's "validation is a safety feature". An empty document parses to the zero value (no error). Cross-file/completeness checks are deferred to T1.2.
> - **Durations**: `config.Duration` (a `time.Duration` with `UnmarshalYAML`) parses Go-duration strings (`2h`, `30m`, `168h`); used by `Budget.Wall`, `SandboxLimits.Wall`, `JetStreamConfig.MaxAge`.
> - **`nats.jetstream` modeled minimally** as `JetStreamConfig{Replicas, MaxAge}` — concrete stream defs (subjects/retention) stay in `internal/messaging`; retention/replicas/max-age are OPEN in messaging.md, so only env-varying knobs surface in infra. `sandbox.limits.mem` kept as a string ("2Gi", k8s-style quantity) — parsed by the sandbox backend (T1.6), not the loader.
> - The loader does NOT validate cross-file references (role→soul resolution, produces/on_failure targets, persona existence) — that is **T1.2 `harness validate`**, the next task.

> **T1.2 findings (2026-05-28):**
> - `(*config.Config).Validate() error` in `internal/config/validate.go` is the startup gate. It **accumulates all problems** (not fail-fast) into a `*ValidationError{Problems []string}` (sorted, deterministic) — operators fix config once and re-run. Checks: stage shape (each stage is agent XOR non-agent, i.e. `role` xor `kind`); `produces`/`on_failure` targets defined; role↔soul both ways (every agent-stage role has ≥1 soul, every soul's role is used by some stage); soul name uniqueness; selector keys/values non-empty; persona files exist on disk (via `PersonaPath`); pre/postcondition references known; produces-graph acyclic + reachable; **plus** model-registry cross-check (every `soul.model` ∈ `infra.models`; openai-compat entries need an endpoint) — the latter was flagged as validate's job in load.go's doc comments.
> - **Condition registry**: the condition-expression language is OPEN (shell vs CEL), so validate gates pre/postconditions against an explicit registry in validate.go: bare preconditions `{blockers-closed}`, bare postconditions `{tests-red-then-green, tests-pass, gosec, deps-scan}`, and comparison form `<metric><op><number>` with known metrics `{mutation}` (e.g. `mutation>=0.8`). **Extend these sets as new conditions/metrics land** (affects T1.19 gate runner). An unrecognized guard is treated as a typo and fails loud.
> - **Reachability model**: roots = stages with produces-indegree 0 (in the canonical DAG that's *both* `requirements` AND `plan`, since the config has no `requirements→plan` produces edge — the seed issue enters at `plan`). A pure DAG always has every node reachable from its indegree-0 roots, so the reachability check's real catch is a cycle-isolated component (reported alongside the cycle). `on_failure` self-routes (e.g. `implement.on_failure: implement`) are role-flow feedback, NOT produces edges, so they do **not** trip the cycle check — only `produces` edges are walked.
> - **NOT wired into the CLI yet** — `Validate()` is a library function with full unit coverage; `cmd/harness/main.go` is still the version stub. The `harness validate` subcommand (arg parsing) is deferred to **T1.21** (`harness` CLI), which the plan already scopes for `validate`/`run`/`seed` together.
> - Env note: in this Linux dev box `golangci-lint` v2.12.2 lives in `$(go env GOPATH)/bin` but is NOT on `PATH` by default — `make check` needs `export PATH="$PATH:$(go env GOPATH)/bin"` first (T0.1 findings described the darwin/brew install). All 41 unit tests pass; `make check` clean.

- [x] **T1.1 Config schema + loader** — structs + YAML unmarshal for `harness.yaml` (dag + policy), `souls/*.yaml`, `infra.<env>.yaml`; persona paths resolved to markdown files. ([configuration.md](specs/configuration.md))
- [x] **T1.2 `harness validate`** — startup gate: every DAG role resolves to ≥1 soul; every `produces:`/`on_failure:`/condition target is defined; persona files exist; selectors well-formed; DAG not unreachable/trivially-looping. Fail loud. ([configuration.md](specs/configuration.md))
- [x] **T1.3 beads read integration** — wrap `bd` to query ready work (no open blockers + precondition holds) and read issue fields into `Issue`/`Brief`. ([components/orchestrator.md](specs/components/orchestrator.md))

> **T1.3 findings (2026-05-28):**
> - `internal/beads.Client` shells out to the `bd` CLI (no Go library — bd owns its own db/versioning; funneling all access through this one package is what keeps the single-writer invariant enforceable). Read methods: `Ready(ctx) ([]core.Issue, error)` → `bd ready --json --limit 0` (bd's `ready` already applies blocker-aware "open + no active blocker" semantics; `--limit 0` drops the default 10-row page); `Get(ctx, id) (core.Issue, error)` → `bd show <id> --json`. Constructor `New(WithBinary, WithDir)` — `WithDir` sets the cwd bd auto-discovers `.beads/` from.
> - **Role-storage convention (load-bearing for T1.4):** the harness role/stage is stored in bd issue **metadata** under key `role` (`beads.MetadataKeyRole`), NOT in labels. Rationale: the spec uses an issue's *labels/tags* for soul-`selector` matching (Phase 3), a distinct concept from the stage binding that routes an issue to `work.<role>`. Verified `metadata` round-trips as a JSON object through `bd ready`/`show --json` (bd 0.62.0). `core.Issue.Role` is populated from `metadata.role`; missing/non-string metadata → empty Role (read path stays robust, never errors on foreign metadata). **T1.4 must write the role via this same metadata key** (`bd update <id> --metadata '{"role":"..."}'` or on create).
> - bd JSON field map → `core.Issue`: `id`→ID, `title`→Title, `description`→Body, `metadata.role`→Role. bd emits a JSON *array* for ready/list AND for `show` (one-element array). bd writes `--json` to stdout, advisory warnings to stderr — the client parses stdout only and folds stderr into the error on non-zero exit. `core.Issue` was left unchanged (no Status/Tags field added yet — not needed for the ready→dispatch read path; model them in T1.4 when status transitions land).
> - Testing: unit tests drive the decode/mapping via an injectable `run` seam with canned bd JSON (fast, no bd needed); integration tests (`TestReadyIntegration` etc.) drive the **real `bd` binary** in a temp db — they `t.Skip` if `bd` is absent from PATH but run here. bd quirks learned: a temp dir whose name contains a `.` is rejected by bd as an "invalid database name" (`t.TempDir()` paths are fine); `bd q <title>` is the quiet create that outputs only the new ID. All 53 unit+integration tests pass; `make check` clean.
- [x] **T1.4 beads single-writer transitions** — orchestrator-only status writes with lease/TTL on `in_progress`; apply validated proposals (create child issues + `blocked-by` edges) with acyclicity check. ([components/orchestrator.md](specs/components/orchestrator.md), [architecture.md](specs/architecture.md))

> **T1.4 findings (2026-05-28):**
> - Single-writer transition methods added to `internal/beads.Client` in `transitions.go` (same package as the T1.3 reader — funneling all writes through one type is the single-writer invariant): `Claim(ctx, id, ttl) (time.Time, error)`, `Release(ctx, id)`, `Close(ctx, id)`, `Block(ctx, id)`, and `Apply(ctx, []core.Proposal) ([]core.Issue, error)`. All map to `bd update --status …` / `bd create` / `bd dep add` / `bd delete`.
> - **Lease/TTL model:** `Claim` sets status `in_progress` and stamps `metadata.lease_until` = now+ttl as an RFC3339 UTC timestamp (`beads.MetadataKeyLease = "lease_until"`), via `bd update <id> --status in_progress --set-metadata lease_until=<ts>`. `--set-metadata` **merges** single keys (verified) so the `role` written at creation is preserved. bd has a native `--claim` verb but it's owner-based with **no TTL**, so the harness models the lease explicitly in metadata — the reconcile sweep (T1.19) reads `lease_until` to find stranded `in_progress` work. `Release` clears it with `--unset-metadata lease_until` and reopens. Reading the lease back into a struct is deferred to T1.19 (tests read it via raw `bd show` for now; `core.Issue` still has no Status/lease field).
> - **Acyclicity is enforced by bd itself**: `bd dep add` rejects a cycle-creating edge at add time (`Error: adding dependency would create a cycle`) — verified. The current `core.Proposal` shape (a brand-new child with only outgoing `DependsOn` edges to *existing* issues) can never form a cycle by construction, so the check is defense-in-depth for any future proposal shape that adds edges among existing issues. `Apply` is **all-or-nothing**: on any create/edge failure it deletes every issue it created in the call (`bd delete <id> --force`), so the orchestrator can safely retry — proven by `TestApplyRollbackIntegration` (a dep on a nonexistent id fails the whole batch and leaves zero new issues).
> - bd CLI specifics learned: `bd create --json` emits a **single object** (not an array, unlike `ready`/`show`); `bd delete` needs `--force` to act (without it prints an interactive "DELETE PREVIEW"); `bd update --status` accepts open/in_progress/blocked/closed and closing needs no session id; `bd list --all --json --limit 0` lists every issue incl. closed (used for rollback count assertions). `bd create --deps 'type:id'` exists but `Apply` creates-then-`dep add`s to reuse the proven cycle-rejecting path with clean rollback.
> - Testing: unit tests assert exact `bd` arg shapes and the rollback/validation control flow via the injectable `run` seam; integration tests drive real `bd` (claim stamps lease + preserves role; Release/Close/Block transitions; Apply creates children with roles and blocked-by edges so a blocked child is excluded from `Ready`). 68 unit+integration tests pass; `make check` clean.
- [x] **T1.5 Sandbox interface** — microVM-shaped from day one: explicit rootfs/worktree seeding, no casual bind mounts, local-socket I/O, resource limits, deterministic teardown. ([components/sandbox.md](specs/components/sandbox.md))

> **T1.5 findings (2026-05-28):**
> - `internal/sandbox/sandbox.go` defines two interfaces + value types (no concrete backend — that's T1.6). `Backend.Provision(ctx, Spec) (Sandbox, error)` is the factory (backend chosen by config, `sandbox.backend`). `Sandbox` is the live handle: `ID() string`, `Exec(ctx, Command) (ExecResult, error)`, `Teardown(ctx) error`.
> - **microVM-shaped design choices (the point of doing this before T1.6):** (1) **no host-worktree-path / copy-out method** — a microVM has no host path, and the candidate branch leaves only via the runner's brokered git push, so the runner never reaches into the sandbox; code goes *in* via `Exec` only. (2) **No general mounts/network field on `Spec`** — the single `Spec.Broker Endpoint` (the local socket) is the ONLY route out, which is how "zero-network, no casual bind mounts" is enforced *by construction*. (3) `Exec` reports a non-zero `ExitCode` in `ExecResult`, NOT as the Go error — error is reserved for "couldn't run at all", so callers distinguish "tests failed" from "couldn't run tests". (4) `Teardown` is documented idempotent + safe on a partially-provisioned sandbox (the runner reaps in a deferred path).
> - **Single-source-of-truth decision:** `Spec.Limits` reuses `config.SandboxLimits` directly rather than defining a parallel `sandbox.Limits` (no adapter — the runner passes loaded config limits straight through). This makes `sandbox` import `config` (clean layering: sandbox→config→core, no cycle). `Endpoint{Network, Address}` abstracts the transport ("unix"+path for Docker, "vsock"+"cid:port" for Firecracker) so the Sandbox interface stays backend-agnostic; the runner stands up the broker listener and tells the backend where it is.
> - `Spec.Validate()` is a fail-loud boundary check (aggregates all problems into a sorted `*InvalidSpecError`, mirroring `config.ValidationError`): requires profile, workspace repo + base ref, broker network + address, CPU>0, non-empty Mem, and **Wall>0** (an unbounded sandbox would defeat the termination guarantee). Disk is optional (not every backend meters it). A backend may assume a validated Spec is sane.
> - **Config gap fixed:** added the optional `Disk string yaml:"disk,omitempty"` field to `config.SandboxLimits` — `specs/components/sandbox.md` documents `disk: 8Gi` but T1.1 omitted it, so strict parsing would have rejected the documented infra YAML. Guarded by `TestLoadInfraDiskLimit`.
> - Tests: `Spec.Validate` table (happy path + each missing field + all-problems-sorted), plus a fake `Backend`/`Sandbox` pair with compile-time `var _ Backend/Sandbox` assertions exercising the full provision→exec→teardown lifecycle (proves the contract composes; a real backend is T1.6). 83 tests pass; `make check` clean.
- [x] **T1.6 Docker sandbox backend** — implements T1.5: seed writable worktree at brief base ref, unix-socket transport to runner, enforce limits, unconditional teardown. ([components/sandbox.md](specs/components/sandbox.md))

> **T1.6 findings (2026-05-28):**
> - `internal/sandbox/docker.go` adds `DockerBackend` (+`NewDockerBackend`/`WithDockerBinary`) implementing the T1.5 `Backend`/`Sandbox` contract by **shelling out to the `docker` CLI** via an injectable `run dockerRunFunc` seam — same pattern as `beads.Client` (no Docker Go SDK: it's a huge transitive tree and Docker is local-dev-only; the production target is Firecracker). The seam returns `(stdout, stderr, exitCode, launchErr)` so a non-zero command exit is data, not a Go error — `launchErr` is reserved for "couldn't run docker at all".
> - **Provision**: validates spec; requires `Broker.Network=="unix"` (vsock is Firecracker) and that `Broker.Address` **already exists and is not a dir** (a missing bind-mount source makes Docker auto-create an empty *directory* in the socket's place, silently breaking the one channel out — fail loud instead). Boots `docker run -d --init --network none --cpus N --memory <bytes> -w /workspace -v <sock>:/run/harness/broker.sock <profile> sleep 2147483647`. `--network none` is the zero-network invariant by construction; the single socket mount is the deliberate Docker analog of vsock. `keepAliveSeconds` (~68y, max int32) keeps the container alive for repeated `docker exec`; accepted by both GNU and busybox `sleep`.
> - **Profile→image**: the `Spec.Profile` string is used **directly as the Docker image ref** (identity map). The per-role rootfs/toolchain catalog is the OPEN "rootfs composition" question in sandbox.md — deferred.
> - **Worktree seeding**: host-side `git clone --no-hardlinks <repo> <tmp>` + `git checkout <baseRef>`, then `docker cp <tmp>/. <id>:/workspace`. A **full clone** (not `git archive`) so the seeded worktree has a writable `.git` — the agent commits a candidate branch the runner later pushes via the broker. Explicit `cp` seeding, never a live bind mount of the host repo (the microVM-shaped rule). Seeding is behind a `prepareWorktree` seam so Provision arg-shapes unit-test without real git. On seed failure the container is torn down (proven by `TestProvisionSeedFailureTearsDown`).
> - **Mem limits**: `parseQuantity` converts k8s-style quantities (`2Gi`,`512Mi`,`1G`,bare bytes) to a byte count for `--memory` (binary Ki/Mi/Gi/Ti/Pi = 1024ⁿ, decimal k/K/M/G/T/P = 1000ⁿ; integer numeric part only). **Disk is NOT enforced** by the Docker backend — Docker disk quotas need a specific storage driver (`--storage-opt size=`); disk is optional in the contract, left to the Firecracker backend.
> - **Wall-clock = termination guarantee**: Docker has no native wall-kill, so the backend enforces it itself — a watchdog goroutine reaps the container when `Limits.Wall` elapses (`TestWatchdogReaps`), AND each `Exec` is bounded by the remaining wall budget (refuses outright once past the deadline: `TestExecWallExhausted`).
> - **Exec**: `docker exec -w <workdir> [-e KEY=VAL]... [-i] <id> <path> <args...>`; `cmd.Dir` joins under `/workspace`. Docker exit **125** (CLI/daemon couldn't exec, e.g. container gone) is mapped to a Go error so callers distinguish "sandbox dead" from "command failed"; 126/127 pass through as `ExitCode` since a wrapped program can also produce them.
> - **Teardown**: `docker rm -f <id>`, idempotent via `sync.Once` + a `done` channel that also stops the watchdog; safe on a partially-provisioned (id-less) sandbox.
> - Tests: 12 unit tests (seam-injected, no daemon: parseQuantity table, Provision arg shapes/rejections/rollback, Exec arg shapes + exit-code/125/launch-err/wall mapping, Teardown idempotency, watchdog) + `TestDockerSandboxIntegration` (real docker+git, `t.Skip` if absent or daemon unreachable; busybox container proves seeded worktree at base ref, writable `.git`, mounted broker socket, **zero-network via `/sys/class/net == [lo]`**, non-zero-exit-is-result, deterministic teardown). 46 sandbox tests pass; `make check` clean. Integration test **ran** here (docker 29.4.0, busybox pulled), not skipped.
- [x] **T1.7 Broker protocol** — local-socket RPC framing + request/response types for the brokered calls: model completion, git push, event publish. Deny-by-default; everything else rejected. ([messaging.md](specs/messaging.md), [components/runner.md](specs/components/runner.md))

> **T1.7 findings (2026-05-28):**
> - `internal/broker` is the **protocol/transport layer only**; the actual relaying (calling the adapter, real git push, NATS publish) is deferred to **T1.12** behind a `broker.Handler` interface the runner implements. Splitting it keeps the security-critical deny-by-default dispatch unit-testable before any adapter exists.
> - **Wire framing** (`protocol.go`): one length-prefixed JSON frame per message — 4-byte big-endian length + JSON body. Envelopes are `Request{Method, Params json.RawMessage}` / `Response{Result json.RawMessage, Error *Error}`; Params/Result stay raw so the envelope is method-agnostic and the server only decodes params **after** the allow checks pass. `maxFrameSize = 64 MiB`, checked on read **before allocating** — the server reads from the untrusted sandbox, so the attacker-controlled length prefix must not be able to OOM the runner. `readFrame` uses `io.ReadFull` so a truncated/closed conn is a loud error.
> - **One request per connection** (`Client.roundTrip` dials, writes one request, reads one response, closes; `Server.handleConn` reads one, dispatches, writes one, closes). No in-band correlation/multiplexing needed — the reply on a connection is unambiguously the reply to its single request. Unix-socket connect is cheap (local), so per-call dial is fine.
> - **Deny-by-default is two gates** in `Server.dispatch`: (1) **method allowlist** — the `Method` set is closed (`model.completion`/`git.push`/`event.publish`); an unknown method → `CodeUnknownMethod`, handler never called. (2) **destination allowlist** — each method maps to an egress token (`llm-api`/`git`/`nats`) via `Method.destination()`, checked against `WithAllowlist(...)` (sourced from `config.BrokerConfig.Allowlist`); not listed → `CodeDenied`. **No allowlist option = deny everything** (the secure default). Error codes distinguish broker rejections (`unknown_method`/`denied`/`bad_request`) from a brokered call that ran and failed (`handler_error`).
> - **Transport**: `Listen(network, address)` is split from `Serve(ctx, ln)` so the caller binds synchronously (socket exists) before accepting in a goroutine — kills the listen/dial race in tests and in the runner. Only `"unix"` is supported; `"vsock"` (Firecracker) and other networks fail loud (Phase 5). `Listen` removes a stale unix socket first (runner is sole owner of the ephemeral per-invocation path). `Serve` closes `ln` on ctx-cancel and treats the post-close Accept error as clean shutdown (needs `//nolint:nilerr`).
> - **`model.Request` defined now** (`internal/model/request.go`) — this **resolves the T0.3 deferral**. It is the canonical model-call input the broker carries: `Messages []Message` (incl. a leading `RoleSystem` turn — no separate System field, single representation of history), `Tools []ToolDef`, `MaxTokens int`. Optional capability fields (prompt caching, extended thinking) are still **T1.8**'s to add — additive, not a migration. `broker.Client.Complete` carries `model.Request`→`model.Response` directly (no wrapper). Git push / event publish have self-contained payloads (`GitPushRequest{Branch}`→`GitPushResult{Commit}`, `PublishRequest{Type, Payload}`→ack).
> - **`broker` package dependency-light**: takes `(network, address)` strings, NOT `sandbox.Endpoint`, so it stays a generic local-socket RPC with no `sandbox` import (the runner, which holds both, does the trivial `Endpoint`→strings mapping). Imports only `internal/model`.
> - Tests (`broker_test.go`, white-box `package broker`): full client↔server round-trips over `net.Pipe` + a fake `Handler` (Complete/GitPush/PublishEvent, incl. asserting the request survives the boundary unchanged); `dispatch` unit tests for every reject path (unknown method, destination-not-allowed, default-deny-all, bad params, handler-error propagation); framing tests (round-trip, oversize-header rejection, truncated body); and a **real unix-socket integration test** (`Listen`+`Serve`+real `net.Dial`, exercising all three calls + an unknown method over the wire). No external binary → not skipped. 13 broker tests; 128 total unit+integration pass; `make check` clean.
- [x] **T1.8 Provider adapter interface + Anthropic adapter** — canonical ↔ Anthropic wire (messages, tool defs, tool_use/tool_result, usage), streaming first-class. ([models.md](specs/models.md))

> **T1.8 findings (2026-05-28):**
> - **`model.Adapter` interface** (`internal/model/adapter.go`): `Complete(ctx, Request, onEvent StreamHandler) (Response, error)`. Streaming is first-class **in the interface** (not a retrofit): adapters always consume the provider's streaming API and report text deltas via `StreamHandler` (`StreamEvent{TextDelta}`); a nil handler still drains+assembles. The interface lives in `model` (SDK-free, interface only); concrete adapters are subpackages. A tool_use turn is a normal `Response` with `Stop=StopToolUse`, **not** an error — error means the call couldn't complete.
> - **`model.Request` reconciled with the spec inner-loop** (`agent.md`: `request := {system, messages, tools}`): added a top-level `System string` field and **removed the redundant `RoleSystem`** role (was a second way to express system → ambiguous for adapters; single source of truth). System maps to Anthropic's top-level `system` param and (T1.9) an OpenAI leading system message. Roles are now `user`/`assistant`/`tool`. Updated `model_test.go` + `broker_test.go` accordingly. **MaxTokens 0 = "adapter default"** (the adapter supplies one; Anthropic requires a positive max_tokens) — budget enforcement is T1.16's job, not a wire concern.
> - **Anthropic adapter** (`internal/model/anthropic`, package `anthropic`, SDK imported as `sdk` to dodge the name collision): thin wrapper over the **official `github.com/anthropics/anthropic-sdk-go v1.45.0`** (now a direct dep; `go mod tidy` pulled gjson/sjson/jsonschema etc. as indirect). `New(modelName, ...option.RequestOption)` — options pass straight through to `sdk.NewClient`, so the runner injects the key via `option.WithAPIKey` (key held only by runner, never config; SDK falls back to `ANTHROPIC_API_KEY`). One adapter is bound to one model name (registry T1.10 builds one per `soul.Model`).
> - **Translation specifics**: canonical `Message` → SDK message per role — `RoleUser`→user text block; `RoleAssistant`→assistant text + `NewToolUseBlock(id, Args, name)` (Args is `json.RawMessage`, marshals to itself so tool args cross unchanged); **`RoleTool`→a *user* message of `tool_result` blocks** (Anthropic has no tool role — tool results live inside a user message). `ToolDef.Params` (a full JSON Schema) is decomposed into `ToolInputSchemaParam{Properties, Required}` with **leftover top-level keys preserved in `ExtraFields`** so nothing in the author's schema is dropped. Response: text blocks concatenate into `Text`, `tool_use` blocks → `ToolCalls`, `Usage` normalized (incl. `CacheCreation/ReadInputTokens`), stop reason mapped (`refusal`→`StopContentFilter`; unknown/`pause_turn` pass through as the raw string, lossless).
> - **SDK v1.45.0 API notes** (verified against module source, NOT memory — the old `anthropic.F(...)` builder style is gone): params are plain fields + `param.Opt[T]` set via helpers (`anthropic.String/Int/Float`); `MessageNewParams{Model (string alias), MaxTokens int64, System []TextBlockParam, Messages []MessageParam, Tools []ToolUnionParam}`; tools via `ToolUnionParam{OfTool: &ToolParam{...}}`; content built with `NewTextBlock/NewToolUseBlock/NewToolResultBlock` returning `ContentBlockParamUnion`; streaming via `client.Messages.NewStreaming(...)` → `*ssestream.Stream[MessageStreamEventUnion]`, drained with `for stream.Next() { acc.Accumulate(stream.Current()) }` (**`(*sdk.Message).Accumulate` is a method**, assembles the final message incl. usage/stop); text deltas read via `event.AsAny().(ContentBlockDeltaEvent).Delta.AsAny().(TextDelta).Text`. Response unions read via `block.AsAny()` type switch (`TextBlock`/`ToolUseBlock`; `ToolUseBlock.Input` is `json.RawMessage`).
> - **Testing without network**: translation is pure and split out — `toParams` is tested by marshaling to wire JSON and asserting the object Anthropic receives (roles, tool_use/tool_result, input_schema); `fromMessage` is tested by `json.Unmarshal`-ing an API-shaped payload into `sdk.Message` (real parse path — SDK types have apijson `UnmarshalJSON`) then asserting the canonical `Response`. `fromStopReason`/`toInputSchema` table-tested. One live `TestCompleteIntegration` (cheap `claude-haiku-4-5-20251001` call, asserts streamed deltas + usage) **skips unless `ANTHROPIC_API_KEY` is set** — skipped here. 7 anthropic tests; 134 pass + 1 skip total; `make check` clean.
- [x] **T1.9 OpenAI-compatible adapter** — covers OpenAI + local Ollama/vLLM via `endpoint`; canonical ↔ `tool_calls`/`tool` role + usage normalization. *(v1 per models.md; parallelizable with T1.8.)* ([models.md](specs/models.md))

> **T1.9 findings (2026-05-28):**
> - **OpenAI adapter** (`internal/model/openai`, package `openai`, SDK imported as `sdk`): thin wrapper over the **official `github.com/openai/openai-go/v3 v3.37.0`** (now a direct dep). Mirrors the T1.8 anthropic adapter exactly — same `New(modelName, ...option.RequestOption)` constructor (one adapter per model name; registry T1.10 builds one per `soul.Model`), same streaming-first `Complete`, same pure `toParams`/`fromCompletion` translation split. **The single "OpenAI-compatible" adapter covers OpenAI + Ollama/vLLM/Together**: the backend is chosen by the runner passing `option.WithBaseURL(endpoint)` (+ `option.WithAPIKey`); no per-vendor code. Key held only by runner; SDK falls back to `OPENAI_API_KEY`.
> - **Major-version note**: openai-go is at `/v3` (latest major; `@latest` of the bare path resolves to v1.12.0 — do NOT use). Module path is `github.com/openai/openai-go/v3`; `option` at `.../v3/option`. `shared.FunctionDefinitionParam`/`FunctionParameters` are re-exported in the root package as `sdk.FunctionDefinitionParam`/`sdk.FunctionParameters`, so no `shared` import is needed.
> - **Translation specifics (the divergences from Anthropic, all absorbed here):** (1) **System** → a leading `sdk.SystemMessage(req.System)` (OpenAI has no separate system channel — unlike Anthropic's top-level `system` param). (2) **RoleAssistant with tool calls** → a `ChatCompletionAssistantMessageParam` (content via `Content.OfString`, tool calls via `ToolCalls []ChatCompletionMessageToolCallUnionParam{OfFunction: &...FunctionToolCallParam{ID, Function{Name, Arguments}}}`), wrapped in the message union's `OfAssistant`. **Arguments is a JSON *string*** (OpenAI), not a JSON object (Anthropic `tool_use.input`) — canonical `Args json.RawMessage` is `string()`-cast on the way out and `json.RawMessage([]byte)`-wrapped on the way back. Empty Args → `"{}"` so the wire stays valid. (3) **RoleTool expands to N messages** — OpenAI carries each tool result as its own `tool`-role message keyed by `tool_call_id` (`sdk.ToolMessage(content, id)`), the inverse of Anthropic nesting all results in one user message; `toMessageParams` therefore returns a `[]union`. **No `is_error` flag** on an OpenAI tool message (Anthropic has one) — error signal lives in the content text. (4) **Tools** → OpenAI takes the **full JSON Schema verbatim** as `function.parameters` (`map[string]any`); no properties/required decomposition (Anthropic's `toInputSchema` work is unnecessary here).
> - **Usage normalization**: `prompt_tokens`→InputTokens, `completion_tokens`→OutputTokens, `prompt_tokens_details.cached_tokens`→CacheReadTokens. **Key divergence:** OpenAI's `prompt_tokens` *includes* cached tokens (cached is a subset — reported for visibility, NOT additive), whereas Anthropic excludes cache reads from `input_tokens` and bills them separately. OpenAI has no separate cache-creation billing, so `CacheCreationTokens` stays 0. The canonical `Usage` shape normalizes both — budget enforcement (T1.16) treats InputTokens as the billed prompt count for each.
> - **Streaming usage gotcha**: OpenAI only returns token usage on a stream when `stream_options.include_usage=true` is sent — `toParams` always sets `StreamOptions: {IncludeUsage: sdk.Bool(true)}` so the runner can tally budgets. Accumulation via `var acc sdk.ChatCompletionAccumulator; acc.AddChunk(chunk)`; final completion is the embedded `acc.ChatCompletion`. Text deltas read from `chunk.Choices[0].Delta.Content`. `client.Chat.Completions.NewStreaming(ctx, params)` → `*ssestream.Stream[ChatCompletionChunk]`.
> - **MaxTokens**: uses `MaxCompletionTokens` (NOT the deprecated `max_tokens`, which the o-series reasoning models reject; vLLM honors max_completion_tokens). Unlike Anthropic (which *requires* a positive cap, so it forces a 4096 default), **OpenAI does not require an output cap**, so canonical `MaxTokens==0` leaves the field **off the wire** ("let the server default") rather than inventing a ceiling — termination is the runner's budget job, not this knob. Guarded by `TestToParamsOmitsMaxTokensWhenUnset`.
> - **finish_reason mapping** (`fromFinishReason`, plain strings): `stop`→StopEndTurn, `length`→StopMaxTokens, `tool_calls`/`function_call`→StopToolUse, `content_filter`→StopContentFilter, unknown passes through raw (lossless). Response tool-call union (`ChatCompletionMessageToolCallUnion`) can be `function` or `custom`; non-function calls are skipped (we only define function tools).
> - Tests (`openai_test.go`, mirrors anthropic): wire-shape (marshal `toParams`→JSON, assert system message + assistant tool_calls + per-result tool messages + verbatim-schema function def), max-tokens-omitted-when-unset, empty-args→`{}`, unknown-role-rejected, `fromCompletion` (unmarshal API-shaped JSON into `sdk.ChatCompletion`, assert canonical Response incl. cached-tokens subset), no-choices safety, finish-reason table. One gated `TestCompleteIntegration` (`OPENAI_API_KEY`; `OPENAI_BASE_URL`/`OPENAI_MODEL` override for local Ollama/vLLM) — skipped here. 7 openai unit tests; **141 unit+integration pass + 2 skip** (anthropic + openai live tests); `make check` clean.
- [x] **T1.10 Model registry + resolution** — map `soul.model` → adapter (+ endpoint) from `infra.<env>.yaml`; keys from env, never config. (needs T1.8) ([configuration.md](specs/configuration.md), [models.md](specs/models.md))

> **T1.10 findings (2026-05-28):**
> - New package `internal/model/registry` (package `registry`). **Why its own package, not `internal/model`:** it constructs the concrete `anthropic`/`openai` adapters, which import `internal/model` — putting the registry in `model` would cycle. So `registry` imports `config` + `model` + both adapter subpackages; `model` itself stays internal-dependency-free. No cycle (`config`→`core` only; `config` does not import `model`).
> - **API: `New(map[string]config.ModelProvider) (*Registry, error)` + `(*Registry).Adapter(modelName string) (model.Adapter, error)`.** Keyed by model name (= `soul.Model`); the runner resolves with `reg.Adapter(soul.Model)`. Deliberately does **not** take `core.Soul` (keeps registry decoupled from `core`; the `.Model` deref is the caller's trivial job).
> - **Eager construction** at `New` (one adapter per entry; fail loud on unknown provider / endpoint-less openai-compat) rather than lazy — consistent with config validation being a startup gate. `sdk.NewClient(...)` makes **no network call**, so eager build is cheap and tests need no key/daemon. Adapters are reused across invocations.
> - **Keys from env, never config — implemented explicitly here**, not left to SDK env fallback: `build()` reads `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` (exported as `registry.EnvAnthropicKey`/`EnvOpenAIKey`) and passes via `option.WithAPIKey` **only when set**, so keyless local backends (Ollama/vLLM) still resolve. openai-compat also gets `option.WithBaseURL(endpoint)`. The two SDKs have **distinct `option` packages** (aliased `anthropicopt`/`openaiopt`), so each provider branch builds its own option slice — no shared option type.
> - **Provider strings are now a single source of truth**: added `config.ProviderAnthropic/ProviderOpenAI/ProviderOpenAICompat` constants in `infra.go`, used by both `registry.build()` and `config.validate.go`. Tightened `validateModels` to a `switch` that **rejects an unknown provider** (a typo like `antropic` previously passed validate and only failed at registry build) — fail-loud at the startup gate, with the registry's `default:` case as defense-in-depth for a config that skipped validation.
> - **Testing (`registry_test.go`, external `registry_test` package — tests the public API as the runner will consume it):** asserts each provider routes to the correct **concrete adapter type** (`*anthropic.Adapter` / `*openai.Adapter` via type assertion — endpoint/key plumbing is opaque inside the SDK client and already covered by T1.8/T1.9 integration tests, so the registry test proves only the routing); unknown-model lookup errors; unknown-provider errors; endpoint-less openai-compat errors; empty/nil map → registry that resolves nothing. 5 registry tests; **146 unit+integration pass + 2 skip** (anthropic+openai live tests); `make check` clean.
- [x] **T1.11 Runner: pull + provision** — JetStream pull consumer on its role(s); provision sandbox (T1.6), seed worktree, inject Brief + scoped git token; ack only after harvest (ack = the lease). ([components/runner.md](specs/components/runner.md))

> **T1.11 findings (2026-05-28):**
> - `internal/runner` (was just `doc.go`) now has `runner.go`: `Runner` + `Options` + `New(...)` + `Run(ctx)`. The runner's own lifecycle (pull → provision → broker socket → invoke → publish result → reap → ack) is **fully implemented**; the two collaborators it cannot own yet are **injected seams**: `broker.Handler` (relay logic = T1.12) and a new `runner.Invoker` interface (`Invoke(ctx, sandbox.Sandbox, core.Brief) (core.Result, error)` = the agent inner loop, T1.13). This is the clean DI boundary the plan's atomic split implies — not stubs.
> - **Work-message contract:** `harness.work.<role>` carries a **JSON-marshaled `core.Brief`** (default Go field-name encoding; producer and consumer share the type, no tags needed). The orchestrator (T1.19) will marshal `core.Brief` to publish; the runner `json.Unmarshal`s it. Result harvest publishes a **JSON `core.Result`** on `harness.result.<role>` via JetStream (the HARNESS_RESULT stream from T0.4) — minimal harvest; artifact-store evidence-by-hash is T1.18.
> - **Ack = lease, encoded as Ack/Nak/Term:** message Acked **only after** the Result is published (harvest). A provision/invoke/publish failure → `Nak()` (redeliver to another runner — the lease at work). An **undecodable Brief is poison** → `TermWithReason` (a redelivery can't fix bad bytes; terminating avoids an infinite redeliver loop). `AckWait` defaults to `Limits.Wall` when unset and must exceed the longest invocation.
> - **Pull loop:** one durable pull consumer per role via `messaging.EnsureWorkConsumer` (runners compete → horizontal scale). Uses the consumer's `Messages()` iterator + `Next()` in a per-role goroutine; ctx-cancel calls `iter.Stop()`, and the resulting `ErrMsgIteratorClosed`/`ctx.Err()` is treated as clean shutdown (`//nolint:nilerr`, same pattern as `broker.Serve`). The runner does NOT create streams (orchestrator's job / tests call `SetupStreams`); it only ensures its consumers.
> - **Sandbox lifecycle:** per invocation the runner mints a unique unix socket path in `Options.SocketDir` (random hex), `broker.Listen`s it, runs `broker.Serve` with `WithAllowlist(Options.Allowlist)` in a goroutine (canceled after invoke), then builds `sandbox.Spec{Profile: brief.Soul.Sandbox, Workspace:{Repo: Options.Repo, BaseRef: brief.Base}, Limits, Broker:{unix, sock}}` and `Provision`s. **Teardown is unconditional**: deferred on a fresh `context.WithoutCancel(ctx)` + 30s timeout so a canceled/timed-out invocation still reaps (the ephemerality guarantee must not ride the happy path).
> - **"Inject Brief" in bootstrap = handing the Brief to the in-process `Invoker` as a value** (the agent loop is co-located with the runner in the kernel). Physical injection into a separately-running sandboxed agent process is deferred to the Firecracker/agent-binary work. **Scoped git token deferred to Phase 5** (per-task short-lived secret minting) — not stubbed.
> - **`Options.Repo` is a runner-level setting** (where to clone worktrees from): the Brief carries only `Base` (a ref), so the source repo is supplied by the runner. In bootstrap it's the local harness repo path; `cmd/harness run` (T1.21) will source it from config/flags.
> - Testing (`runner_test.go`, white-box `package runner`, real embedded NATS + fakes for Backend/Sandbox/Handler/Invoker): happy path (provision spec shape, result published on result subject, sandbox reaped, message acked); **Nak redelivery** (invoker fails once → redelivered → succeeds, ≥2 invokes); **teardown-always** (invoker always errors → sandbox still torn down); **poison message Term** (undecodable bytes don't invoke and don't block a following valid brief); options validation. 5 runner tests; **151 unit+integration pass + 2 skip**; `make check` clean.
- [x] **T1.12 Runner: broker impl** — relay model completion (via adapter T1.10, runner holds key), git push (task branch only), event publish; log every call; tally `Usage` for budgets. ([components/runner.md](specs/components/runner.md), [security.md](specs/security.md))

> **T1.12 findings (2026-05-28):**
> - `internal/runner/broker_handler.go` adds `relay`, the concrete `broker.Handler` that replaces the T1.11 injected-handler seam. It is built **per invocation** (in `Runner.invoke`), not once at `New`: every brokered call is invocation-scoped — `Complete` uses the adapter resolved from *this* Brief's `soul.Model`, events go to *this* invocation's `harness.agent.<id>.events` subject, and `GitPush` is constrained to *this* task's branch. A single shared handler couldn't bind those, so the shared `handler broker.Handler` param is gone.
> - **Runner constructor change:** `New(opts, backend, resolver AdapterResolver, pub Publisher, invoker, js)`. Two new injection seams on the runner (in runner.go alongside `Invoker`): `AdapterResolver{ Adapter(modelName) (model.Adapter, error) }` (the T1.10 `*registry.Registry` satisfies it; abstracted so the relay tests use a fake adapter) and `Publisher{ Publish(subject, data) error }` (the live agent event/token feed — **core NATS, not JetStream**, per T0.4, so a `*nats.Conn` satisfies it; the result/work plumbing keeps using the `js` handle). `cmd/harness run` (T1.21) wires the real registry + nats conn.
> - **`invoke` reordered:** resolve adapter → `broker.Listen` (bind socket) → `Provision` (needs the socket path) → build `relay` with the live sandbox handle → `broker.Serve(relay)` → `Invoke`. The relay must be built *after* Provision because `GitPush` needs the live `sandbox.Sandbox` to extract the branch. On Provision failure `ln.Close()` unlinks the just-bound socket. The old `socketPath()` became `invocationID()` (random hex) — one id names the socket, the agent event subject, **and** tags the invocation's logs.
> - **Model completion relay:** `Complete` calls `adapter.Complete(ctx, req, onEvent)` where `onEvent` publishes each non-empty `StreamEvent.TextDelta` as a `{"type":"token","delta":...}` event to the agent subject (best-effort — a publish failure is logged at debug and swallowed; the StreamHandler runs on the token-draining goroutine and must never stall/fail a call). On success it accumulates `resp.Usage` into a mutex-guarded running total (`relay.Usage()` — the budget input T1.16 reads; the runner logs the per-invocation total after `Invoke`). A failed call tallies nothing. **Every call is logged** (the audited chokepoint): completion logs stop reason + all four token counts + tool-call count; git push logs branch+commit; event logs type.
> - **Git push (task branch only):** `GitPush` refuses any branch != `runner.CandidateBranch(issueID)` (= `candidate/<issueID>`) — a **new exported convention** that is the single source of truth for the task-branch name; the agent's submit tool (T1.13/T1.15) must commit the candidate onto this ref. This enforces "push only the task branch" (in particular refuses `main`) **without** a scoped token (token minting is Phase 5; in bootstrap the push destination is the local source repo `opts.Repo`). Mechanism, honoring the microVM no-copy-out rule: exec `git bundle create - <branch>` **inside** the sandbox, capture the bundle on `ExecResult.Stdout` (binary-safe `[]byte`), then host-side `pushBundleToRepo` writes a temp bundle file and `git -C <repo> fetch <bundle> +refs/heads/<branch>:refs/heads/<branch>` + `rev-parse` to land the ref and return the head SHA. This is how a candidate becomes reachable to the gate (T1.17) / merge (T1.19) with no bind mount or network. `pushBundleToRepo` is a seam (`relay.pushBundle`) so the relay's orchestration unit-tests without real git.
> - **Event publish:** `PublishEvent` forwards the marshaled `broker.PublishRequest` to the agent event subject; fire-and-forget (the agent holds no NATS creds and doesn't know its subject — the broker supplies it).
> - Testing: `broker_handler_test.go` — relay unit tests with fakes (recordingAdapter/recordingPublisher/bundleSandbox): completion streams deltas + tallies usage across 2 calls + propagates error without tallying; git push refuses non-task branch (no exec, no pushBundle), extracts bundle with correct exec args + passes bytes to pushBundle + returns commit, fails on non-zero bundle exit; publish forwards to the right subject. Plus `TestPushBundleToRepoIntegration` — **real host git, no docker**: builds a candidate branch in one repo, bundles it exactly as the in-sandbox exec would, asserts `pushBundleToRepo` lands branch+SHA in a separate source repo (skips if git absent). runner_test.go's `fakeHandler` → `fakeResolver`/`fakeAdapter`, passing the embedded-NATS `*nats.Conn` as the Publisher. **158 unit+integration pass + 2 skip** (anthropic+openai live tests); `make check` clean.
- [x] **T1.13 Agent inner loop** — boot soul, build context from Brief, ReAct loop {canonical request → broker model call → execute tool calls → append → budget check}; terminate on submit / budget / escalate. ([components/agent.md](specs/components/agent.md))

> **T1.13 findings (2026-05-28):**
> - `internal/agent` (was just `doc.go`) now has `loop.go` + `tools.go`. `agent.Loop` implements the T1.11 `runner.Invoker` seam and is the Go-native ReAct loop: boot soul → build context from Brief → iterate {canonical `model.Request` → brokered `Complete` → execute tool calls → append results → budget check} → terminate on a lifecycle tool (submit/escalate) or budget. `var _ runner.Invoker = (*Loop)(nil)` in the test asserts satisfaction (agent imports runner only in the test; runner does NOT import agent — no cycle, and the loop is structurally injected via cmd/harness in T1.21).
> - **Invoker seam extended** to carry the broker endpoint: `Invoke(ctx, sb sandbox.Sandbox, brief core.Brief, brokerEndpoint sandbox.Endpoint) (core.Result, error)`. The loop is constructed once but the broker socket is per-invocation, so the endpoint must arrive as a call arg. The runner passes `spec.Broker`; updated `runner.invoke` call site + `runner_test.go`'s `fakeInvoker`. The loop dials the runner's own broker socket in-process (faithful loopback over unix) — model calls go loop→`broker.Client`→socket→`broker.Server`→relay→adapter, exercising the real deny-by-default boundary even co-located.
> - **Tools are the T1.14/T1.15 seam, not built here.** `tools.go` defines `Tool` (`Def() model.ToolDef`; `Invoke(ctx, args json.RawMessage) (Outcome, error)`), `Outcome{Content, IsError, Result *core.Result}`, `ToolSource func(Invocation) ([]Tool, error)`, and `Invocation{Sandbox, Broker BrokerClient, Brief}`. **Tool-failure vs fatal split:** a returned `error` from `Tool.Invoke` is fatal to the invocation (runner Naks/redelivers); a tool that merely failed (build error, bad path) reports it via `Outcome.IsError`+`Content` so the model can react — mirrors the sandbox Exec contract (error = couldn't run; non-zero exit = data). A non-nil `Outcome.Result` terminates the loop and becomes the invocation Result (how submit→done, escalate→needs-spec-clarification end it).
> - **Broker decoupling:** `Completer{Complete}` + `BrokerClient{GitPush, PublishEvent}` interfaces; `brokerConn` (their union) is what `Loop.connect(ep)` returns — default `broker.NewClient(ep.Network, ep.Address)`, with `var _ brokerConn = (*broker.Client)(nil)` proving the real client backs it. The same per-invocation conn is the loop's `Completer` AND the tools' `Invocation.Broker` (single connection backs every brokered call). `Loop.readPersona`/`Loop.connect` are unexported seams the white-box test overrides (no real socket/file needed).
> - **Budget = termination (basic enforcement here; T1.16 wires config.Policy + dead-letter).** `agent.Budget{MaxTurns, MaxTokens, MaxOutputTokens}`: `MaxTurns` (default `DefaultMaxTurns=50`) is the loop's own halting guarantee independent of tokens (a model emitting tiny responses can't loop forever); `MaxTokens` caps cumulative input+output tallied from each `Response.Usage`; `MaxOutputTokens`→`Request.MaxTokens` per call (0 = adapter default). **A breach returns `core.Result{Status: StatusFailed}`, NOT an error** — so the runner harvests it and the orchestrator dead-letters it, rather than Nak→redeliver→loop-forever. config.Budget has Tokens/USD/Wall but **no turn field**; the turn cap is the agent loop's own knob (cmd/harness maps config→agent.Budget in T1.21). USD/Wall budgets are not enforced in the loop (Wall is the sandbox watchdog's job, T1.6; USD needs a cost table — deferred).
> - **Soul boot = read `soul.Persona` → `Request.System`.** Empty persona path is a hard error (validate guarantees persona files exist). `readPersona` defaults to `os.ReadFile` (in-process bootstrap reads the host path the Brief carries); once the agent is its own in-sandbox binary the persona content will travel in the Brief instead (Firecracker, Phase 5). `buildContext` renders the Brief (issue id/title/body, base ref, spec slice, criteria) into the opening RoleUser turn + a closing instruction to call submit when ready / escalate on ambiguity.
> - **No-tool-call handling:** if a Response has zero tool calls, the loop cannot self-submit (no candidate) and must not invent intent — it appends a `nudge` user turn steering back to submit/escalate and continues; the turn cap bounds a model that keeps refusing. **Evidence/PromptSHA** assembly (provenance) is left to the lifecycle submit tool (T1.15) + T1.20; loop-built failure Results are minimal (Status only) by design.
> - Testing (`loop_test.go`, white-box `package agent`, fakes for conn/tool/sandbox): submit terminates + first-request shape (System=persona, Tools, Brief context fields present); non-terminal tool result appended as RoleTool with correct ToolCallID/Content + assistant tool-call turn carried forward; unknown tool → IsError result (no crash); escalate → needs-spec-clarification; turn-budget exhaustion (exactly MaxTurns calls + nudge present) → failed; token-budget exhaustion → failed; MaxOutputTokens passthrough; model-call error fatal; tool error fatal; persona read error + empty-persona fatal; ToolSource error fatal. 11 agent tests; **169 unit+integration pass + 2 skip** (anthropic+openai live tests); `make check` clean.
- [x] **T1.14 Workspace tools** — `read_file`, `write_file`, `edit_file`, `list_dir`, `search`, `run` (build/test/lint) executing in-sandbox on the worktree. ([components/agent.md](specs/components/agent.md))

> **T1.14 findings (2026-05-28):**
> - `internal/agent/workspace.go` adds `WorkspaceTools(sb sandbox.Sandbox) []Tool` — the six in-sandbox tools, each a `funcTool` (new lightweight `Tool` adapter in `tools.go`: `def model.ToolDef` + closure) capturing `sb`. They all act on the candidate worktree via `sandbox.Sandbox.Exec` (working dir = worktree root); the agent never touches the host. T1.21 (cmd/harness) composes `WorkspaceTools` with the T1.15 lifecycle tools into the `ToolSource` the loop calls.
> - **Implementations (shell-free where possible; positional args, never interpolation):** `read_file`=`cat -- <path>`; `list_dir`=`ls -la -- <path>` (default `.`); `search`=`grep -rnE -- <pattern> <path>` (default `.`); `run`=`sh -c <command>`. `write_file`=`sh -c 'mkdir -p "$(dirname "$1")" && cat > "$1"' sh <path>` with **content on stdin** and **path as $1** — a path can never inject shell and content is never quoted into the command line. `edit_file` is host-side: `cat` the file → `strings.Count`/`Replace` in Go (unique-match required unless `replace_all`; 0 matches or >1-without-replace_all → IsError) → write back via the same `write_file` path. This honors the microVM no-copy-out rule (all I/O via Exec) while giving Edit-tool exact-substring semantics.
> - **Failure model (mirrors the loop's tool contract):** a sandbox that *cannot run* a command → fatal Go error (loop returns error → runner Naks → fresh-sandbox redelivery). A command that *ran and failed* (missing file, red tests, bad args) → `Outcome{IsError:true}` with the text so the model reacts and retries. `run`/`read_file` etc. set `IsError = exitCode != 0` and prefix `exit code N`. **grep exit 1** (no lines selected, empty stderr) is special-cased to a non-error `"no matches"`. Malformed/`{}` args → IsError (model-correctable), not fatal.
> - **Output cap:** `maxToolOutput = 32 KiB`, head kept + `[output truncated]` marker, so a noisy build log or huge `cat` can't blow the context window. Assumes the sandbox profile ships a POSIX shell + coreutils (`sh`/`cat`/`ls`/`grep`/`mkdir`) — true of the bootstrap Docker images and planned role rootfses.
> - Testing (`workspace_test.go`, white-box, `scriptedSandbox` routing Exec by command): tool-set completeness + valid JSON schemas; read (argv shape + missing-file IsError); write (sh+stdin+path-as-arg); edit (unique replace writes substituted content, not-found/not-unique IsError, replace_all); search (grep exit-1 → "no matches", hits + path arg); run (exit-code surfaced + `sh -c` shape, success); invalid/missing args IsError; dead-sandbox fatal; truncation. 15 agent tests added; **184 unit+integration pass + 2 skip**; `make check` clean.
- [x] **T1.15 Lifecycle tools** — `submit`, `escalate` (raise `needs-spec-clarification`), `request_subtask` (propose child issue); assemble the `Result` envelope. ([components/agent.md](specs/components/agent.md))

> **T1.15 findings (2026-05-28):**
> - `internal/agent/lifecycle.go` adds `LifecycleTools(brief core.Brief, brk BrokerClient) []Tool` — the three tools that control the invocation and assemble `core.Result`. **This completes the agent's tool surface** (T1.13 loop + T1.14 workspace + T1.15 lifecycle); cmd/harness (T1.21) composes `append(WorkspaceTools(inv.Sandbox), LifecycleTools(inv.Brief, inv.Broker)...)` into the `ToolSource`.
> - **Shared state via one `*lifecycle` value:** submit/escalate/request_subtask are built from the same `lifecycle{brief, brk, mu, proposals}` so the terminal tools attach proposals gathered across the run. `submit`/`escalate` are **terminal** (return `Outcome.Result` → loop ends); `request_subtask` is **non-terminal** (accumulates a `core.Proposal`, returns ordinary Content, agent keeps working) — so one run can propose several children AND submit its own candidate. takeProposals folds them into whichever terminal Result fires.
> - **submit** pushes via `brk.GitPush(GitPushRequest{Branch: core.CandidateBranch(issue.ID)})` and builds `Result{Status: done, Branch:{Ref, Commits:[commit]}, Proposes}`. The model does **not** choose the branch (the relay refuses any other); the canonical name is used. A **push failure is recoverable, not fatal** → `Outcome{IsError}` with a "commit onto <branch> then submit again" hint (the agent likely hasn't committed yet), so the loop continues rather than Nak-ing the work. `done` is "candidate ready", never acceptance — the gate decides (producer ≠ verifier).
> - **escalate** → terminal `Result{Status: needs-spec-clarification}`; **reason is required** (forces the agent to articulate the ambiguity). `core.Result` has no free-text field, so the reason lives in the **transcript** (artifact store, T1.18) the human reads, not the Result — no spec/struct change needed. **request_subtask** requires title+role; optional body + `depends_on` (existing issue IDs); the orchestrator validates DAG-legality on Apply (T1.4), so the tool just records the proposal.
> - **`CandidateBranch` moved `runner` → `core`** (`core.CandidateBranch`, in result.go next to `Branch`). Both ends of the broker need it (runner relay refuses non-matching branches; agent submit pushes onto it), so it belongs in the dependency-free leaf rather than having `agent` import `runner` (inverted layering). Updated runner.go's two references; tests used string literals so were unaffected. The loop's `buildContext` now also tells the agent the candidate branch name to commit onto before submit.
> - **Evidence/PromptSHA still empty** on submit Results — provenance (PromptSHA) is T1.20 and artifact refs (transcript/gate-output) are T1.18; submit assembles status+branch+proposals only for now.
> - Testing (`lifecycle_test.go`, `recordingBroker` capturing GitPush): tool-set completeness + valid schemas; submit pushes canonical branch + terminal done Result with commit; push failure → recoverable IsError (non-terminal); escalate terminal needs-spec-clarification + reason-required; request_subtask non-terminal accumulation folded into a later submit's Proposes (title/role/depends_on); request_subtask title/role validation. 6 agent tests added; **190 unit+integration pass + 2 skip**; `make check` clean. (Pre-existing gofmt nit in `internal/beads/transitions_test.go` is unrelated and not flagged by the lint gate.)
- [x] **T1.16 Budget enforcement** — per-invocation turn/token caps enforced from tallied `Usage`; breach → stop/escalate. (The kernel's termination guarantee.) ([workflow.md](specs/workflow.md))

> **T1.16 findings (2026-05-28):**
> - The per-invocation enforcement itself already shipped with the agent loop (T1.13): `agent.Loop.Invoke` caps `MaxTurns` (default `DefaultMaxTurns=50`), cumulative `MaxTokens` (input+output tallied from each `Response.Usage`), and per-call `MaxOutputTokens`→`Request.MaxTokens`. T1.16's remaining, deferred piece was the **config→`agent.Budget` wiring** + nailing down breach semantics against the (then-unbuilt) orchestrator contract. No new enforcement code was needed in the loop.
> - **`agent.BudgetFromPolicy(config.Policy) Budget`** (`internal/agent/budget.go`) is the single source of truth for the mapping, so `cmd/harness run` (T1.21) calls `agent.New(tools, agent.BudgetFromPolicy(cfg.Harness.Policy), log)` rather than re-deriving it. The mapping is **deliberately narrow** because the specs define two distinct budget scopes (workflow.md "two levels"): `config.Policy.Budget` is the *per-issue cumulative* ceiling (summed across the on_failure feedback loop — the orchestrator's to enforce/dead-letter, T1.19), whereas `agent.Budget` is the *per-invocation* cap. Only the **token** dimension carries over: per-issue `Budget.Tokens`→per-invocation `MaxTokens` as a conservative hard ceiling (one invocation can never exceed what the whole issue is ever allowed; 0 stays uncapped, MaxTurns still bounds). `turns` is the loop's own knob (config has no turn field — T1.13 decision — so it defaults via `New`), `wall` is the sandbox watchdog's (T1.6), `usd` needs a per-model cost table (deferred). `agent` importing `config` is acyclic (config→core only; agent already pulls config transitively via sandbox).
> - **Breach semantics confirmed against orchestrator.md, not changed:** a per-invocation breach returns `core.Result{Status: StatusFailed}` (the loop "stops" — it is the *secondary* halting guarantee that one invocation always terminates). It is NOT itself a dead-letter and NOT `needs-spec-clarification`: the reconciliation loop (orchestrator.md:53–56) decides retry-via-`on_failure` vs dead-letter from `retryable(issue)` = retry cap + cumulative budget — the *primary* termination guarantee, T1.19. So the failure-taxonomy "budget breach → Escalation → dead-letter" is the orchestrator's cumulative check, reached when a `failed` result's issue is no longer retryable. The loop must not pre-empt that decision (a single turn-cap hit may be a transient stuck-model that a fresh retry clears). Updated the `Budget` doc comment in loop.go to record this two-level split.
> - Testing: `budget_test.go` — `TestBudgetFromPolicy` (per-issue tokens→MaxTokens; usd/wall/turns NOT carried), `TestBudgetFromPolicyZeroBudget` (zero policy → uncapped tokens, and `New` still defaults MaxTurns so the loop terminates). The existing loop budget tests (turn/token exhaustion → failed, MaxOutputTokens passthrough) already cover enforcement. **192 unit+integration pass + 2 skip** (anthropic+openai live tests); `make check` clean.
- [x] **T1.17 Gate runner** — fresh verification sandbox (orchestrator-controlled, distinct from producer's), clean checkout of candidate branch, run `build` + `test`, report pass/fail. Producer ≠ verifier. ([verification.md](specs/verification.md))

> **T1.17 findings (2026-05-28):**
> - `internal/gate/gate.go` adds `gate.Runner` (was just `doc.go`): `New(backend sandbox.Backend, checks []Check, socketDir string, log)` + `Run(ctx, Candidate) (Report, error)`. It provisions a **fresh verification sandbox** via the injected `sandbox.Backend` seeded at the **candidate branch ref** (`Candidate.Ref` = `core.CandidateBranch(issueID)`), runs the ordered `[]Check` in it, and returns a per-check `Report`. Producer ≠ verifier holds **by construction**: the gate seeds a brand-new worktree from the repo the candidate was pushed to (T1.12's `pushBundleToRepo` landed `refs/heads/candidate/<id>`), so the verifier never shares the producer's sandbox/state, and the orchestrator (T1.19) — not the agent — calls the gate.
> - **`Check{Name, Cmd}`** is operator config, NOT agent input: each check is run `sh -c <Cmd>` at the worktree root and passes iff exit 0. The kernel runs `build` then `test`; the set is data so Phase 2 adds mutation/scanner checks without touching the runner. The gate is **fail-fast**: it stops at the first failing check (a failed build makes a test run meaningless) — `Report.Checks` holds results up to and including the failure. An **empty check list is a hard error** (a checkless gate would pass everything — defeats verification).
> - **Error vs verdict split (mirrors the sandbox Exec contract, load-bearing for T1.19 routing):** `Run` returns a Go `error` only when the gate could not reach a verdict (provision failed, sandbox died mid-check) — a *transient/infra* failure the orchestrator **retries**. A clean `Report{Passed:false}` is a real *gate failure* the orchestrator routes via `on_failure`. Conflating them would make a flaky daemon look like a code defect (or vice-versa).
> - **Verification sandbox has zero egress — enforced by construction:** `sandbox.Spec` requires a broker endpoint (and the Docker backend requires the socket to exist), so the gate mints a per-gate unix socket via `broker.Listen` and serves a **deny-all** broker (`broker.NewServer(denyHandler{}, broker.WithAllowlist(nil))`). The verifier can make no model calls, no git push, no events — it only runs build/test on a clean checkout. `denyHandler` errors on every method as defense-in-depth (never reached: an empty allowlist denies every destination before the handler). Teardown is unconditional (deferred, `context.WithoutCancel` + 30s), same ephemerality guarantee as the runner.
> - **Captured stdout/stderr per check is the gate evidence** the provenance trail attaches; large items go to the artifact store by hash (plan T1.18). Output is not truncated here — the sandbox wall limit bounds runtime/size, and truncating evidence is worse than a big Report; T1.18 owns hashing/persistence.
> - Testing: `gate_test.go` (scripted fake Backend/Sandbox, no docker) — all-pass (green report + seeded ref = candidate branch + profile + unix broker endpoint + reaped), fail-fast (only the first check runs, captured exit/stderr, reaped), Exec-error → infra error (still reaped), no-checks → error (zero provisions), provision-failure → error. `gate_integration_test.go` — **real docker + git** (skips if absent; ran here): seeds a repo whose `candidate/issue-1` branch has a `marker.txt` absent from `main`, leaves HEAD on main, and asserts a `grep -q candidate marker.txt` check passes (proving the gate checked out the candidate, not main) plus that an `exit 7` check fails the gate. **200 unit+integration pass + 2 skip** (anthropic+openai live); `make check` clean.
- [ ] **T1.18 Artifact store interface + files backend** — content-addressed local files; runner harvests transcript + gate evidence before teardown, referenced by hash. (Minimal — full evidence set comes in Phase 2.) ([components/artifact-store.md](specs/components/artifact-store.md))
- [ ] **T1.19 Orchestrator loop** — reconciliation control loop: query ready → dispatch `work.<role>` → consume Result → validate proposals → gate (T1.17) → on accept: fast-forward merge + apply `produces:`; on reject: `on_failure`/retry; on breach: dead-letter (`harness.dlq` + mark blocked); sweep expired leases. Idempotent throughout. ([components/orchestrator.md](specs/components/orchestrator.md), [workflow.md](specs/workflow.md))
- [ ] **T1.20 Provenance trailer on merge** — write `Soul | Model | Issue | Prompt-SHA | Verified:` trailer; SHA/evidence hashes point into the artifact store (T1.18). ([security.md](specs/security.md), [integration.md](specs/integration.md))
- [ ] **T1.21 `harness` CLI** — `validate`, `run` (in-process orchestrator + one runner), `seed` (CLI stand-in for the wizard: write spec + create seed issue via the single-writer path). End-to-end: one spec → merged commit. ([bootstrap.md](specs/bootstrap.md))

**→ Self-host point reached.** From here the harness can implement Phases 2–5 as
beads issues, human reviewing diffs ("trusted-dev mode" per bootstrap.md).

---

## Phase 2 — Independent verification *(coarse)*

Earns no-human-review for non-TCB work. ([verification.md](specs/verification.md))

- [ ] Independent `author-tests` stage — a soul distinct from `implement` writes failing acceptance tests (defends against correlated errors).
- [ ] Red→green proof as a first-class postcondition (fail on base, pass on impl).
- [ ] Mutation testing postcondition + minimum-score gate. *(OPEN: score + operators.)*
- [ ] Independent scanners — `gosec` (SAST), dependency/vuln/license scans, policy-as-code.
- [ ] Test↔spec traceability map — per-test spec heading+sentence, harvested as evidence.
- [ ] Trusted-dev policy profile — human-approval postcondition for the self-hosting transition. *(OPEN, configuration.md.)*
- [ ] *(OPEN)* Second different-model reviewer soul in `qa` (N-version diversity).

## Phase 3 — Full DAG, decomposition & merge queue *(coarse)*

- [ ] Decomposition planner soul (`plan` stage) — reads seed issue + spec, emits `implement` issues with dependency edges.
- [ ] Emergent breadth — child-issue proposals validated for DAG-legality before write. ([workflow.md](specs/workflow.md))
- [ ] Spec slice resolution + context horizon — referenced file + linked neighbours to configured depth. ([specs-process.md](specs/specs-process.md))
- [ ] Spec-version pinning + recompile-the-delta — pin slice content hash per Brief; on spec edit, invalidate/re-derive affected issues. ([specs-process.md](specs/specs-process.md))
- [ ] Serialized merge queue — rebase onto current `main`, **re-gate the merged result**, conflict → sandboxed resolution issue. ([integration.md](specs/integration.md)) *(OPEN: integrate as its own role vs orchestrator function.)*
- [ ] Role→soul `selector` matching by issue tags; per-role model tiers. ([configuration.md](specs/configuration.md))

## Phase 4 — Control room *(coarse)*

Stack: templ + Tailwind standalone CLI + `embed.FS` + htmx/Alpine + SSE. ([control-room.md](specs/control-room.md), [observability.md](specs/observability.md))

- [ ] Web server scaffold + asset embedding (`go generate`: `templ generate` + Tailwind CLI).
- [ ] Board (kanban over beads, live via NATS→SSE).
- [ ] DAG view (server-side SVG, hover/drill via Alpine).
- [ ] Activity feed (NATS events → SSE).
- [ ] Issue/invocation detail (Brief, transcript, candidate diff, gate evidence, budget, retries).
- [ ] Dead-letter queue view — the primary human action surface.
- [ ] Budgets view (OTel metrics) + Provenance view (commit → issue→soul→model→prompt→evidence).
- [ ] Requirements **wizard** — steered conversation + alignment ledger + spec authoring + explicit-approval consent gate; Create and Resolve as one component. Trusted, not sandboxed. ([specs-process.md](specs/specs-process.md))
- [ ] OTel spans at broker/orchestrator/runner → trace backend; **replay** of an invocation's decision trail.

## Phase 5 — Production isolation & distribution *(coarse)*

- [ ] Firecracker sandbox backend (vsock transport, KVM microVM) — production target. ([components/sandbox.md](specs/components/sandbox.md))
- [ ] *(optional)* gVisor backend (medium-trust).
- [ ] Vetted package mirror/proxy — supply-chain mediation: pin, scan, log fetches. ([security.md](specs/security.md))
- [ ] Scoped short-lived secret minting — per-task git token that pushes only the task branch. ([components/runner.md](specs/components/runner.md))
- [ ] Distributed NATS (external cluster, JetStream replicas/retention) + runners across hosts.
- [ ] S3/MinIO artifact backend. ([components/artifact-store.md](specs/components/artifact-store.md))
- [ ] Provenance signing + key custody. *(OPEN, security.md.)*
- [ ] *(optional)* Warm sandbox pools; HA orchestrator via NATS-KV leader election. *(OPEN.)*

---

## Open decisions affecting the plan

These are `OPEN:` in the specs and may reshape tasks above:

- Mutation score threshold + operators (T2 mutation gate).
- `integrate` as its own role/soul vs. orchestrator-owned with sandboxed conflict help (Phase 3).
- HA orchestrator: single instance (fine for v1) vs. leader election (Phase 5).
- Condition-expression language for pre/postconditions (shell exit-code vs. CEL) — affects T1.2/T1.19.
- Exact module set drawn into the TCB boundary — must be pinned before autonomy is switched on for harness work.
